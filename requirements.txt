# Core LiveKit dependencies
livekit-agents>=0.8.0
livekit>=0.11.0
livekit-api>=0.7.0

# LiveKit plugins for STT, TTS, LLM, and VAD
livekit-plugins-deepgram
livekit-plugins-cartesia
livekit-plugins-openai
livekit-plugins-silero
livekit-plugins-groq
# Environment variable management
python-dotenv

# ASGI server (if needed for web API endpoints)
uvicorn[standard]

# Logging (usually included in Python standard library, but listed for clarity)
# logging is part of Python standard library
